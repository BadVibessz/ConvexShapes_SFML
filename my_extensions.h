#pragma once
#include <string>
#include <vector>

using namespace std;
class MyExtensions
{
public:
	static vector<string> Split(string str, char delimiter);
	static int Random(int a, int b);
      
};

